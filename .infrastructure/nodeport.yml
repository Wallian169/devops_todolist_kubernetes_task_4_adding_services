apiVersion: v1
kind: Service
metadata:
  name: todolist-nodeport-service
  namespace: todolist
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
