apiVersion: v1
kind: Service
metadata:
  name: todolist-service
  namespace: todolist
spec:
  selector:
    app: todolist
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
